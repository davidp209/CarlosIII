window.onload=function(){

    const tVector = [2,3,5,6,8,7];
   let x = prompt("Introduce un numero");
   function numIgual(x)
   {
       for( i =0; i < tVector.length; i ++ )
   
       if(tVector[i] == x){
           res = i;
           
       }else {
           res = -1;
           
       }}
   console.log(numeros.find(numIgual));
   
   }